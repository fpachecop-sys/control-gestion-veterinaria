import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Control Gestion Veterinaria',
  webDir: 'www'
};

export default config;
